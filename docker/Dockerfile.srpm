# Lightweight image for SRPM generation.
# Used by GitHub Actions to build .src.rpm files before submitting to COPR.
# Does NOT compile anything — actual compilation happens inside COPR.

FROM fedora:latest

RUN dnf install -y \
        git \
        make \
        python3 \
        rpm-build \
        copr-cli \
    && dnf clean all
